# Memory Repository Rules

## 目的

本仓库只保存项目进度、事实、决策、阻塞事项和下一步行动，供不同电脑之间恢复工作上下文。

## 记录要求

- 先写事实和当前状态，再写判断和下一步。
- 每次更新注明日期，使用 `YYYY-MM-DD` 格式。
- 项目记录放在 `projects/<project-name>/`。
- 重要决策放在 `decisions/`，交接信息放在 `handoffs/`。
- 不记录密码、令牌、密钥、`.env` 内容或未脱敏的个人信息。
- 不把临时缓存、构建产物和大型二进制文件提交到仓库。

